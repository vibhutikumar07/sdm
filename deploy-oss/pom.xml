<project>
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.sap.cds</groupId>
    <artifactId>sdm-root</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <name>Deploy to OSS</name>
    <description>This artifact can be used to deploy all required artifacts of sdm to OSS Nexus</description>

    <properties>
        <maven.deploy.skip>true</maven.deploy.skip>
        <maven.install.skip>true</maven.install.skip>
    </properties>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-install-plugin</artifactId>
                <version>3.1.3</version>
                <executions>
                    <execution>
                        <id>install-sdm</id>
                        <phase>install</phase>
                        <goals>
                            <goal>install-file</goal>
                        </goals>
                        <configuration>
                            <groupId>${project.groupId}</groupId>
                            <artifactId>sdm</artifactId>
                            <packaging>jar</packaging>
                            <file>../sdm/target/sdm.jar</file>
                            <sources>../sdm/target/sdm-sources.jar</sources>
                            <javadoc>../sdm/target/sdm-javadoc.jar</javadoc>
                            <pomFile>../sdm/.flattened-pom.xml</pomFile>
                            <version>${revision}</version>
                        </configuration>
                    </execution>
                    <execution>
                        <id>install-sdm-root</id>
                        <phase>install</phase>
                        <goals>
                            <goal>install-file</goal>
                        </goals>
                        <configuration>
                            <groupId>${project.groupId}</groupId>
                            <artifactId>sdm-root</artifactId>
                            <packaging>pom</packaging>
                            <file>../.flattened-pom.xml</file>
                            <pomFile>../.flattened-pom.xml</pomFile>
                            <version>${revision}</version>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-gpg-plugin</artifactId>
                <version>3.2.6</version>
                <executions>
                    <execution>
                        <id>deploy-sdm</id>
                        <phase>deploy</phase>
                        <goals>
                            <goal>sign-and-deploy-file</goal>
                        </goals>
                        <configuration>
                            <groupId>${project.groupId}</groupId>
                            <artifactId>sdm</artifactId>
                            <packaging>jar</packaging>
                            <file>../sdm/target/sdm.jar</file>
                            <sources>../sdm/target/sdm-sources.jar</sources>
                            <javadoc>../sdm/target/sdm-javadoc.jar</javadoc>
                            <pomFile>../sdm/.flattened-pom.xml</pomFile>
                            <version>${revision}</version>
                        </configuration>
                    </execution>
                    <execution>
                        <id>deploy-sdm-root</id>
                        <phase>deploy</phase>
                        <goals>
                            <goal>sign-and-deploy-file</goal>
                        </goals>
                        <configuration>
                            <groupId>${project.groupId}</groupId>
                            <artifactId>sdm-root</artifactId>
                            <packaging>pom</packaging>
                            <file>../.flattened-pom.xml</file>
                            <pomFile>../.flattened-pom.xml</pomFile>
                            <version>${revision}</version>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
